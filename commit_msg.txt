fix(build): resolve TypeScript unused import errors in App and Schema

- Removed unused React import from App.tsx.
- Removed unused boolean import from schema.ts.
- Verified successful production build (tsc + vite build).
